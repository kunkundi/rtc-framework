build/
install/
.vscode/
.xmake/
third_party/
/yolo*.onnx
/yolo*.pt
models/*.engine
models/*.onnx
models/*.plan
models/*.pt
models/*.trt*

# third_party large binaries — download via script instead



# test data (large binary files)
test_data/zjlabs.yuv
test_data/*.pcm

# lib binaries (managed separately)
lib/*.dll
lib/*.so
lib/*.lib

# dockerfiles dependencies
signaling-server/dockerfiles/

# local packages for upload (do not commit)
scripts/packages/
